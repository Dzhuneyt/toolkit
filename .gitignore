# OS
.DS_Store

# Editor cruft
*.swp
*~

# Node
node_modules/

# Claude Code machine-local settings (the rest of .claude/ is tracked)
.claude/settings.local.json

# claude-notify: real ntfy topic is a secret; only the .example is committed
claude-notify/ntfy.env
